/** Phase 3: Trigger ISR revalidation on the frontend */
export async function revalidateFrontend(_path: string) {
  // TODO Phase 3: Call frontend revalidation endpoint
  // const url = `${process.env.FRONTEND_URL}/api/revalidate?secret=${process.env.REVALIDATE_SECRET}&path=${path}`
  // await fetch(url, { method: 'POST' })
}
